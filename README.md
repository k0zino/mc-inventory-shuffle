## 四个命令，需要 OP 权限（GAMEMASTER 级别或更高）：

- /invshuffle on              # 开启，每10秒打乱所有在线玩家的背包
- /invshuffle off             # 关闭
- /invshuffle status          # 查看当前是否开启 + 间隔时间
- /invshuffle interval 5      # 改成每5秒打乱一次（范围1~3600秒）
- 打乱不包含盔甲栏，副手
